apply plugin: 'application'
mainClassName = 'uk.gov.ida.cloudhsmtool.CloudHSMTool'

dependencies {
    implementation(
            configurations.common,
            configurations.cloudhsmtool
    )
    testImplementation(
            configurations.testing
    )

}