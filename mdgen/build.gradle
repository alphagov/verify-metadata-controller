apply plugin: 'application'
mainClassName = 'uk.gov.ida.mdgen.MetadataGenerator'

dependencies {
    implementation(
            configurations.common,
            configurations.mdgen,
            project(':cloudhsmtool')
    )
    testImplementation(
            configurations.testing
    )
}